enum TapType {
  tap,
  secondaryTap,
  longPress,
  doubleTap,
  none,
}

enum MenuAlignment {
  topLeft,
  topCenter,
  topRight,
  centerLeft,
  center,
  centerRight,
  bottomLeft,
  bottomCenter,
  bottomRight
}

enum MenuPosition { inside, outside }
